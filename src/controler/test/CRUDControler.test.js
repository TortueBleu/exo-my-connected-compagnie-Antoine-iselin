//const request = require("supertest");
const { controllerAdd } = require("../CRUDControler");

describe("test CRUD", () => {

    test("it should reponse a table", () => {
        console.log(controllerAdd);
        //controllerAdd()
    });
});